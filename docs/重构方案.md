# 基于 PaperQA2 重构「可声明原创」新项目 —— 方案

> 输入事实：`/home/wby/PaperQA` = Future-House/paper-qa 的 fork（origin `Wangbaoyun/AgenticRAG4paper`，
> upstream `Future-House/paper-qa`，HEAD `0a03fb0`），Apache-2.0，Copyright 2024 FutureHouse。
> 上游核心代码规模：`src/paperqa` 11,839 行 + `packages/*` 2,234 行 ≈ 1.4 万行 Python。

---

## 0. 结论速览

**能重构，但"完全声明为原创"必须先精确界定它指什么。**

| 声明内容 | 可行性 | 说明 |
| --- | --- | --- |
| "新项目的全部源代码由我独立编写" | ✅ 可达 | 洁净室重写后，代码著作权归你 |
| "模块划分、架构、命名、Prompt 是我的设计表达" | ✅ 可达 | 表达层只要不抄，就是你的 |
| "三段式 Agentic RAG / RCS / 元数据一等公民 是我发明的" | ❌ 不可达 | 这是 PaperQA2 论文（arXiv:2409.13740）的公开贡献，评审一查即知 |
| "复制源码/prompt 后改名，然后声明原创" | ⛔ 不可做 | 违反 Apache-2.0 §4，且构成著作权侵权，"原创"声明直接失效 |

一句话：**"代码原创"可达，"思想原创"不可达。**
最优解不是"换皮"，而是 **洁净室重写（同构不同源）+ 明确的原创增量**——功能与逻辑一致，
工程思想一脉相承，但你有一份真实、可辩护、且面试得分更高的原创贡献。

---

## 1. 法律与事实底线（先确认再动手）

### 1.1 许可证约束
上游为 **Apache License 2.0**（LICENSE 附录第 189 行：`Copyright 2024 FutureHouse`），
仓库内**没有 NOTICE 文件**，4 个 PDF 解析子包各自带 LICENSE。

Apache-2.0 允许复制、修改、再分发（含商用、可闭源），**但**要求：

1. 向接收者提供 License 副本（§4a）；
2. 修改过的文件必须带显著"已修改"声明（§4b）；
3. 保留源码中所有版权、专利、商标、归属声明（§4c）；
4. §6 商标条款：**不得使用 "PaperQA" 名称与标识**。

→ 只要复用了任何一行上游代码，就必须保留 `Copyright 2024 FutureHouse`。
→ **"复用代码"与"完全声明原创"在逻辑上互斥，没有中间地带。**

### 1.2 "思想 / 表达二分法"
著作权只保护**表达**，不保护**思想、方法、算法、功能**（TRIPS 协定 9(2)、中国著作权法、
美国版权法 §102(b)）。因此：

- ✅ 自由使用：三阶段 Agentic RAG 流程、RCS（检索后 LLM 打分+摘要）、MMR 去冗余、
  双索引互补、元数据多源聚合与降级、配置哈希决定索引名、"模型无关"抽象——这些是**方法**。
- ⛔ 不可照搬：源码文本、docstring、注释、**Prompt 字符串**、测试数据、文档正文——这些是**表达**。

### 1.3 必须 100% 重写的"表达"清单（高危区）

| 文件 | 行数 | 风险 |
| --- | --- | --- |
| `src/paperqa/prompts.py` | 241 | ⛔ 极高：Prompt 是典型受保护的创作性表达，逐字/近似改写都危险 |
| `src/paperqa/core.py` 的 `llm_parse_json` | ~130 | ⛔ 高：正则修复策略是其特色表达 |
| 全部 docstring / 行内注释 | — | ⛔ 高 |
| `tests/stub_data/*`、`tests/cassettes/*.yaml` | — | ⛔ 高：论文 PDF、VCR 录制数据 |
| `docs/2024-10-16_litqa2-splits.json5` | 1 MB | ⚠️ 需单独确认：该数据来自 LabBench/LitQA2，可能有独立许可 |
| `README.md`、`CITATION.cff` | — | ⛔ 高 |
| 引用标记格式 `pqac-xxxxxxxx` | — | ⚠️ 需换成自己的方案（也是原创性的可见标识） |
| CLI 名 `pqa`、配置目录 `~/.pqa` | — | ⚠️ 换成自己的 |

> 你现有的 `src/paperqa/types_notes.py`（1,918 行中文源码注释）是对**上游代码**的注释，
> 属于衍生表达。新项目里不要搬运；如需保留，只能作为**独立的学习笔记仓库**，不入新项目。

---

## 2. 三条路线对比

| 路线 | 做法 | 可声明原创 | 主要风险 | 工期 |
| --- | --- | --- | --- | --- |
| **A 改名换皮** | fork 后全局重命名、删版权头 | ❌ 不能 | ⛔ 高：Apache 违约 + 侵权 + 面试/评审穿帮 | 1 天 |
| **B 洁净室同构重写** | 只依据规格 + 论文 + 黑盒行为重写，功能对齐 | ✅ 代码原创；思想需注明出处 | 低 | 3–6 周 |
| **C 洁净室 + 原创增量**（推荐） | B + 3–4 项自己的真实增量 | ✅✅ 可强声明"独立设计与实现，并做了 X/Y/Z 改进" | 最低，面试得分最高 | 5–8 周 |

### 为什么推荐 C，而不是"最大化伪装成原创"

你的目标是**校招简历/面试**。PaperQA2 是 FutureHouse 的知名开源项目，AI 岗面试官大概率见过。
一旦被识破"这是 PaperQA2 改名"，性质从"技术能力问题"升级为"诚信问题"，是**一票否决**级事故。

相反，下面这句话 100% 真实、可辩护、且是更强的面试素材：

> "我研读了 PaperQA2 的实现并复现了它的完整链路，在此基础上**独立设计并实现**了一套
> 同构系统（~8k 行），并做了三项自己的改进：混合检索融合、自研 Agent 运行时、中文文献适配。
> 消融实验显示 X 相比基线提升 Y%。"

面试官听到这个，会追问技术细节；听到"我原创了一个 Agentic RAG 系统"，会追问
"你知道 PaperQA2 吗？"——后者是陷阱题。

---

## 3. 推荐方案（路线 C）总览

### 3.1 项目定位（一句话）

> 面向科研文献的**证据可溯源**问答系统：给定一批 PDF，通过"检索 → 取证据 → 合成答案"
> 的智能体流程，产出**每句话都能回溯到原文片段**的答案与参考文献；支持中文文献与国产模型。

### 3.2 命名（必须全新，且不含 `paper-qa` / `pqa` / `PaperQA`）

| 候选 | 含义 | 备注 |
| --- | --- | --- |
| **`scitrace`** | Science + Trace（溯源） | 推荐：短、可搜、语义贴合"可溯源"核心卖点 |
| `evidenceforge` | 证据锻造 | 偏工程感 |
| `paperlens` | 论文透镜 | 好记，但 "paper*" 前缀与上游同族，商标上稍贴边 |
| `zhenglu` / `征据` | 中文品牌 | 适合中文场景差异化 |

→ 同步确定：包名、CLI 名（如 `stc`）、配置目录（如 `~/.scitrace`）、引用标记前缀
（如 `ev-3f9a2c`，替代 `pqac-`）。

### 3.3 差异化定位（让"原创"有实体）

不做"PaperQA 汉化版"，做"**中文科研文献场景 + 可插拔检索内核**"：

1. **中文/国产化**：中文分块与句子边界、CNKI/万方/arXiv 中文元数据、DeepSeek 等国产模型优先、
   本地 embedding 默认。
2. **可替换的检索内核**：把"向量召回 + LLM-RCS 打分"做成**可插拔策略**，内置
   ① 纯向量 + MMR ② BM25 + 向量 RRF 融合 ③ Cross-Encoder 重排，
   用消融实验回答"LLM 重排到底值多少 token"——这是**可发表的原创贡献**。
3. **自研 Agent 运行时**：不依赖 `aviary` / `ldp` / `fhlmi`，自己实现工具协议 + 循环 +
   超时兜底（约 600 行）—— 这一项就切断了与上游最深的一层"工程思想依赖"。
4. **成本与延迟治理**：证据缓存、token 预算闸门、工具并行、模型分级路由。

---

## 4. 模块级重构映射表

| PaperQA 模块 | 行数 | 职责 | 新项目对应 | 同构度 | 你的不同做法 |
| --- | --- | --- | --- | --- | --- |
| `types.py` | 1382 | Doc / Text / Context / PQASession / DocDetails / ParsedText | `domain/`（5 个纯数据模块） | 概念同构 | 用 pydantic 纯模型 + Protocol，去掉 `Embeddable` 混入，domain 层零外部依赖 |
| `docs.py` | 772 | `Docs` 上帝类：aadd / aquery / retrieve / aget_evidence | `pipeline/` + `service/` | 高 | 拆成 **IngestPipeline / Retriever / EvidenceScreener / AnswerSynthesizer** 四个可独立测试的组件，拒绝 700 行上帝类 |
| `core.py` | 400 | JSON 修复 + 单块摘要打分（RCS） | `pipeline/screening.py` | 中 | 自写容错解析（或直接用 `json_repair`）；打分后端可插拔（LLM / Cross-Encoder） |
| `llms.py` | 585 | VectorStore 抽象 + Numpy/Qdrant + MMR | `ports/vector_index.py` + `adapters/indexes/` | 中 | 自实现相似度检索；MMR 与 RRF 并存；索引层与 LLM 层解耦 |
| `readers.py` | 557 | chunk_pdf / parse_text / chunk_text / read_doc | `ports/parser.py` + `adapters/parsers/` + `pipeline/chunking.py` | 中 | 自设计分块策略（结构感知：章节/段落/表格），参数语义自己定 |
| `utils.py` | 716 | 引文解析、BibTeX、重试、哈希 | `util/` 多个小模块 | 低–中 | 按职责拆分；BibTeX 自写（可用 pybtex 做底层） |
| `prompts.py` | 241 | 全部 Prompt | `prompts/` | **必须零重叠** | 从零写中英双语 Prompt；引用约束用自己设计的格式 |
| `settings.py` | 1290 | pydantic-settings 分层 + 索引哈希 + 模型工厂 | `config/` | 思想同构 | 分层自己定（CLI > env > 命名 JSON）；索引指纹字段集自己定义 |
| `agents/*` | ~2000 | 工具 + 环境状态机 + 超时兜底 + tantivy 检索 | `agent/` + `adapters/indexes/fulltext.py` | 中 | **自研 Agent loop**；工具协议自定义；tantivy 用法自写 |
| `clients/*` | 2439 | Crossref / S2 / OpenAlex / Unpaywall / 撤稿 / 期刊质量 | `adapters/metadata/` | 中 | 只用 httpx 直调公开 REST API；合并与降级策略自设计 |
| `sources/clinical_trials.py` | 350 | ClinicalTrials.gov 工具 | 可选 | 低 | 首版不做 |
| `contrib/` | ~540 | Zotero / OpenReview | 可选 | 低 | 首版不做 |
| `packages/*`（4 个解析子包） | 2234 | PyPDF/MuPDF/Docling/Nemotron | `adapters/parsers/` | 低 | 直接封装底层库，不照搬子包结构 |

**最小可行内核估算**：domain + ports + 核心 pipeline + 一个 parser + 一个 vector index +
一个 fulltext index + 元数据 3 个 provider + agent loop + CLI ≈ **6,000–9,000 行**。

### 4.1 目录结构（建议）

```
scitrace/
  domain/          # 纯领域模型，零外部依赖
    source.py      #   Source        ← Doc / DocDetails
    fragment.py    #   Fragment      ← Text
    evidence.py    #   Evidence      ← Context
    session.py     #   Session       ← PQASession
    citation.py    #   引用与 BibTeX
  ports/           # 接口协议（typing.Protocol）
    parser.py  vector_index.py  fulltext_index.py  metadata.py  llm.py
  adapters/        # 具体实现
    parsers/       #   pypdf / pymupdf / docling
    indexes/       #   numpy_vec / tantivy_fts / qdrant
    metadata/      #   crossref / s2 / openalex / unpaywall / retraction
    llm/           #   litellm_backend / openai_compat / local_embedding
  pipeline/        # 编排（自己的设计）
    ingest.py  chunking.py  retrieval.py  screening.py  synthesis.py
  agent/           # 自研运行时
    loop.py  tools.py  state.py  budget.py
  config/  cli.py  service/  util/  prompts/
tests/  docs/  benchmarks/
```

---

## 5. 洁净室工作流（保证"可声明原创"的证据链）

洁净室（clean-room）是业界成熟做法（Compaq 重写 IBM BIOS 即此方法）。单人也适用：

| 阶段 | 动作 | 产出（=证据） |
| --- | --- | --- |
| **S1 规格** | **只**依据论文 arXiv:2409.13740 + 官方文档 + **黑盒运行**（跑原版、观察输入输出与行为），写出行为规格 | `docs/SPEC.md`（功能、接口、边界、错误行为） |
| **S2 冻结** | 规格写完即冻结；此后实现阶段**不再打开上游仓库**；上游仓库移出开发机或仅在只读容器中用于黑盒对拍 | git 时间线、SPEC 冻结 commit |
| **S3 实现** | **只**依据 SPEC 从零写代码；Prompt 从零写；变量名/模块名/接口自定 | 新仓库完整提交历史 |
| **S4 审计** | 自动化反抄袭审计：① 与上游做 8-gram token 重叠扫描 ② `jscpd`/`simian` 查复制粘贴 ③ Prompt 字符串相似度 ④ 依赖树检查（确认未引入 `paper-qa*` 包） | `docs/PROVENANCE.md` + 审计脚本 `tools/similarity_audit.py` |
| **S5 对拍** | 行为契约测试：同一语料 + 同一问题，两边跑，比较**可观测行为**（是否给引用、是否拒答、工具调用序列、结构化字段） | `tests/conformance/` 双跑报告 |
| **S6 声明** | README 写清"设计范式参考 PaperQA2（arXiv:2409.13740），本项目为独立实现" + 你自己的 LICENSE（Apache-2.0 或 MIT，版权写你） | 合规且更强的原创证据 |

> **关键**：S6 的致谢**不会**削弱你的原创声明。代码著作权 100% 归你；
> 而"承认参考了公开工作"在工程/学术上是加分项——它证明你知道 prior art。
> 反过来，**隐瞒**才是唯一会致命的操作。

---

## 6. 原创增量清单（挑 3–4 项，这就是"原创"的实体）

| # | 增量 | 为什么算原创 | 实现成本 |
| --- | --- | --- | --- |
| 1 | **自研 Agent 运行时**（替换 aviary/ldp） | 工具协议、循环、状态机、超时兜底、成本闸门全部自设计 | 中（~600 行） |
| 2 | **可插拔检索内核 + 消融**：向量+MMR / BM25+向量 RRF / Cross-Encoder 重排 | 用实验回答"LLM 重排的边际收益"，上游没有这个对照 | 中 |
| 3 | **中文科研文献适配**：中文分块、中文引用格式、中文元数据源 | 上游纯英文导向，中文场景是真空白 | 中 |
| 4 | **混合全文索引 + 增量重建**：manifest 哈希增量索引 + 失败降级 | 上游有类似机制但实现路径不同，可做得更简单可测 | 中 |
| 5 | **评测体系**：LitQA2 子集 + 自建中文评测集 + 可复现实验脚本 | 自主评测设计本身即是贡献 | 中–高 |
| 6 | **成本治理**：证据缓存 / token 预算 / 模型分级路由 | 上游基本没有 | 低 |

---

## 7. 里程碑与验收

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| **M0** | 规格与洁净室纪律 | `SPEC.md` 冻结；反抄袭审计脚本就绪；新仓库初始化 |
| **M1** | 骨架：domain + ports + config | 领域模型单测通过；`stc config show` 输出命名配置；索引指纹可计算 |
| **M2** | 摄入链路：PDF → 分块 → 嵌入 → 双索引 | 一篇 PDF 落库；重启后索引可复用；改配置触发重建 |
| **M3** | 元数据管线 | Crossref/S2/OpenAlex 并发补全；单源失败自动降级；撤稿标记可用 |
| **M4** | 检索 + 证据 | 向量召回 + MMR/RRF + 打分筛选；`stc search` 返回带分数片段 |
| **M5** | 答案合成 + 引用绑定 | `stc ask` 产出带内联引用的答案 + 参考文献；证据不足时明确拒答 |
| **M6** | Agent 运行时 | 工具调用循环跑通；超时/异常兜底；确定性"无 Agent 模式"可复现 |
| **M7** | 服务化：CLI + 会话持久化 + 缓存 | 历史问答可检索；缓存命中率可观测 |
| **M8** | 评测与消融 | LitQA2 子集 + 中文自建集跑通；三种检索策略对照表 |
| **M9** | 审计 + 文档 + 开源 | 相似度审计报告（与上游 8-gram 重叠率 < 阈值）；README/架构文档/演示 |

> 依赖清理提醒：上游核心依赖 `fhaviary`、`fhlmi`、`ldp`。新项目**不要引入**这三个包
> （否则"自研 Agent 运行时"这一原创点不成立）。LLM 接入直接用 `litellm` 或 `openai` SDK + httpx。

---

## 8. "逻辑一致"如何证明（需求 2 的落地）

"功能、逻辑、工程实现思维一致"不靠代码相似，靠**行为契约**：

```python
# tests/conformance/test_behavior_contract.py（示意）
CASES = [
    # (语料, 问题, 期望的可观测行为)
    ("paper.pdf", "这篇论文的核心贡献是什么？",
     {"has_inline_citation": True, "has_references": True, "refusal": False}),
    ("paper.pdf", "作者昨天晚饭吃了什么？",
     {"refusal": True, "refusal_phrase_present": True}),
]
```

对拍维度：① 三段式阶段顺序 ② 是否产出引用与参考文献 ③ 拒答行为
④ 证据评分过滤阈值语义 ⑤ 索引重建触发条件 ⑥ 工具集与调用语义
⑦ 配置优先级 ⑧ 错误降级路径。

这套"行为等价"测试**本身就是原创工作**，也是简历上"我如何验证重构等价性"的答案。

---

## 9. 风险与对策

| 风险 | 等级 | 对策 |
| --- | --- | --- |
| "参考"变成"照抄"（写着写着打开上游代码） | ⛔ 高 | S2 阶段物理隔离上游仓库；审计脚本进 CI |
| Prompt 被认为抄袭 | ⛔ 高 | 中文场景重写 + 引用格式换新；审计脚本单独扫 prompt |
| 依赖 `paper-qa-*` 包 | ⚠️ 中 | 依赖树检查进 CI |
| 使用 `PaperQA` 名称/`pqa` CLI | ⚠️ 中 | Apache §6 商标条款；全新命名 |
| 用了 LitQA2 数据集 | ⚠️ 中 | 单独核查其许可；否则只用自建评测集 |
| 工期失控（1.4 万行上游） | ⚠️ 中 | 严格按 M0–M9 剪裁：`clinical_trials`、`contrib`、多模态富化首版全砍 |
| 与现有 fork 混淆 | ⚠️ 低 | 新项目独立仓库；旧 fork `AgenticRAG4paper` 保留为"复现+源码研究"项目，两者定位分开写进简历 |

---

## 10. 待决策问题

1. **新项目的首要目标是哪个？** ① 校招简历/面试作品 ② 规避归属义务、闭源商用 ③ 纯技术练兵
   —— 决定投入规模与是否开源。
2. **是否接受在 README 中注明"设计范式参考 PaperQA2（arXiv:2409.13740）"？**
   （强烈建议接受；拒绝会使整个方案的风险等级从"低"升到"高"。）
3. **原创增量选哪 3 项？** 建议：自研 Agent 运行时 + 可插拔检索内核与消融 + 中文场景适配。
4. **项目命名**：`scitrace` / `evidenceforge` / 自定义？
5. **范围裁剪**：首版是否砍掉多模态（图片/表格富化）、ClinicalTrials、Zotero/OpenReview？
6. **是否保留现有 fork？** 建议保留，作为"开源复现与源码研究"项目；新项目作为"独立设计与实现"项目，
   两个项目在简历上形成"读懂 SOTA → 独立重写 → 做出改进"的完整叙事。
```
