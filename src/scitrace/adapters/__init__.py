"""适配器：``ports`` 中各项契约的具体实现。

本包内的模块是中游唯一允许接触外部世界的层——文件系统、HTTP、第三方库
（pypdf、tantivy、numpy、LLM SDK）都只在这里出现。上层（``pipeline`` / ``agent``）
只看得到 ``domain`` 中的模型。

这条边界的价值不在于"整洁"，而在于**可替换性可被验证**：只要 ``pipeline``
不 import 本包，就可以在测试中用内存实现整体替换掉真实 I/O，
否则每次跑测试都要联网、读盘、加载模型。
"""

__all__: list[str] = []
